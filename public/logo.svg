<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 48" fill="none">
  <!-- Replace this file with the official MoroccoAI logo SVG -->
  <!-- Navy wordmark variant (for light backgrounds) -->
  <!-- Navbar/Footer on dark bg use CSS: filter brightness(0) invert(1) -->
  <text x="0" y="36" font-family="Georgia, serif" font-size="28" font-weight="700"
    letter-spacing="-0.5" fill="#1F2A5C">Morocco
    <tspan fill="#C8202F">.</tspan>AI
  </text>
</svg>
